добавить группы

Kafka key     → влияет на partition (куда пойдёт сообщение)
requestId     → влияет на логику обработки (идемпотентность, трассировка)

добавить kafka ui

ack.acknowledge если успешно обработали (это должно быть в try/catch)



считываю ConsumerRecord стороне payment-service
считанный payload перевожу в CreateOrderEvent
создаю таблицу processedEvents и сохраняю там uniqueOrderNumber из ConsumerRecord для идемпотентности
после списание средств отправляю по ProducerRecord что заказ успешно обработан
принимаю это сообщение на стороне order-service и изменяю в таблице order статус из PENDING на COMPLETED
Сделать CommandHandler который принимает CreateOrderEvent или CancelOrderEvent